summary: "Get Schemas."
description: "Retrieves the SCIM Schemas provided by SRAM."

tags:
    - "SCIM client"

consumes:
    - "application/json"

produces:
    - "application/json"

responses:
    200:
        description: List of SCIM schemas
        schema:
            $ref: "/swagger/schemas/ScimSchemas.yaml"
